class Artist {
	alias: string
	id: string
	adamid: string
	constructor(alias: string, id: string, adamid: string) {
		this.alias = alias
		this.id = id
		this.adamid = adamid
	}
}

export default Artist
